# Unidad de seguridad
- Principios de seguridad
  - Tríada CIA: Confidencialidad (asegurar que la información solo sea accesible por personal autorizado), Integridad (garantizar que la información sea precisa y no haya sido modificada sin autorización) y Disponibilidad (asegurar que la información y los sistemas estén disponibles para los usuarios cuando los necesiten). 
  - Descarga la ISO de una distribución de Linux. En la página de descargas, busca el hash SHA-256 que publican. Una vez descargado el archivo, utiliza un comando (sha256sum en Linux, o Get-FileHash en PowerShell de Windows) para calcular el hash del archivo que has descargado y comprueba que coincide exactamente con el publicado.
- GPO. Contraseñas
  - En un Windows Server, abre el editor de políticas de grupo (gpmc.msc). Edita la "Default Domain Policy" y navega hasta Configuración del equipo > Políticas > Configuración de Windows > Configuración de seguridad > Políticas de cuenta > Directiva de contraseñas. Configura una política que exija una longitud mínima de 10 caracteres y que la complejidad esté habilitada.
- Ufw
  - En una máquina virtual con Ubuntu, instala ufw (sudo apt install ufw). Habilítalo con sudo ufw enable. Por defecto, denegará todo el tráfico entrante. Luego, permite explícitamente el tráfico SSH con sudo ufw allow ssh. Comprueba el estado con sudo ufw status.
- Auditoría y visor de sucesos
