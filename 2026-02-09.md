# Seguridad
- Auditoría y visor de sucesos
  - Ver los logs con Powershell (XML).
```PowerShell
Get-WinEvent -LogName Security -FilterXPath "*[System[(EventID=4688)]]" |
ForEach-Object {
    ([xml]$_.ToXml()).Event.EventData.Data |
    Where-Object Name -eq 'NewProcessName' |
    Select-Object -ExpandProperty '#text'
}
```
